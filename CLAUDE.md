AGENTS.md

Local machine overlay (standalone checkout, 2026-08-19): `.portable/` self-contained toolchain, `dsh.bat` menu launcher, `.env` with `DEEPSEEK_API_KEY` (never read/commit), build complete, headless run OK after removing stale User-level `DEEPSEEK_API_KEY` env var (old key `****3819` overrode `.env` key `****a9bc`). Details in AGENTS.md.